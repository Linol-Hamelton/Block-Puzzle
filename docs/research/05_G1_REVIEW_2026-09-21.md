# Ревью G1 (DEC-0028): независимая проверка работы Gemini

Дата: 2026-09-21. Ревьюер: DeepSeek (`deepseek-71c5c0b3bece591d`). Объект: worktree `D:\Block-Puzzle-g1`, ветка `g1-gameplay`, 36 файлов, +1812/−168, без коммитов. Источники: отчёт Gemini (`g-g1.md`), его журнал `gemini-b022c08d3081ccfc.md`, прямой аудит кода и независимый прогон.

## 1. Независимо воспроизведено (не по словам автора)

| Проверка | Результат |
| :--- | :--- |
| `flutter analyze --fatal-infos --fatal-warnings` | **0 issues** (выполнено ревьюером в worktree) |
| `flutter test` | **All tests passed, 419** (395 базовых + 24 новых; в прогоне видна симуляция anti-chunking) |
| `validate-protocol.ps1` | **PASS, 0 warnings** |
| `protocol-handoff.cjs verify` | **evidence matches the current tree** |
| Симуляция anti-chunking | «5,000 deals with board fill > 50%: max 1 heavy piece and 0 instant deadlocks» |

Замечание: `agy` вернул `status=ERROR`, но это следствие stderr-предупреждения CLI («root agent idle…») и обработки нативных stderr в PowerShell; отчёт, журнал, evidence и работа в дереве — на месте. На содержательность ревью не влияет.

## 2. Статус по пунктам DEC-0028

**Этап A.** A1 Step 6+ (сценарии single/triple, reduced motion, p95 build/raster, 20-мин jank-лог) — реализовано; приборный прогон на Xiaomi **не выполнен** (ожидается). A2 телеметрия (7 событий) — реализовано и покрыто тестами.

**Этап B.** B3 аудио-баланс — проверено в коде: `piece_placed=0.35` + питч 0.96–1.04, `line_clear 0.42/0.50`, `combo 0.48/0.58`, музыка `baseVolume=0.50`. B4 проводка треков — правки экранов на месте. B5 дакинг — проверено: coalescing 110 мс, фактор −1.5 дБ (0.8414), пол 0.70, duck 150 мс + плавное восстановление 250 мс. B6 изотропная тряска — основная очистка: 2.0–4.0 px через `SequenceEffect` (±dx/∓dy) и off при reduced motion; **дефект P2**: All Clear-удар в `_playPerfectClear` остался диагональным `MoveEffect.by(Vector2(4,4))` (`block_puzzle_game.dart:896-902`). B7 тайминги — `ScorePop`/`ComboPulse` по 0.85 с. B8 честный New Best — тесты + карточка 2×2. B9 бесплатный Undo — тест «free undo works with 0 credits, resets combo, second requires credits»; `hasUsedFreeUndo` с дефолтом false и round-trip. B10 AudioFocus — `gainTransientMayDuck` + iOS `mixWithOthers`; приборная проверка ожидается. B11 хаптика — по отчёту (лестница 1–2/3–5/6+), приборно не проверялась. B12 Reduced Motion — проверено: тряска off, вспышка ×0.25, пульсация off. B13 Drop ASMR — проверено (0.96–1.04). B14 KEEP_SCREEN_ON + подтверждение выхода — platform channel и PopScope на месте; проверка на устройстве ожидается. B15 совет после первого Game Over — по отчёту в карточке.

**Этап C.** C16 anti-chunking — проверено в коде (fill>50%: максимум 1 тяжёлая из square3/line5/vline5; первая фигура — fair bag) + симуляция 5000 раздач. C17 Danger Pulse — проверено (fill>75%, цикл 1.5 с, альфа 0.15–0.25, off при reduced motion). C18 All Clear — проверено: празднование без изменения счёта. C19 вербальные тиры — `verbal_tiers.dart` (RU/EN) + центрирование. C20 Throttle Guard — по отчёту и тестам (одна центроидная волна). C21 частицы 6/ячейку + пул — по отчёту; приборный замер ожидается. C22 палитры/SKU — проверено: `utility_tools_pass` фильтруется в магазине; селектор тем + сериализация `selectedTheme`/`musicVolume`/`soundVolume`/`reducedMotion` с обратной совместимостью; тестовая фикстура обновлена осознанно.

## 3. Дефекты и открытые гейты

- **P2-1.** All Clear-тряска диагональная (`block_puzzle_game.dart:896-902`) — привести к изотропной, как в B6.
- **P2-2.** Отслеживаемые `windows/flutter/generated_*` изменены `pub get` — вернуть перед коммитом (шум).
- **P1-процесс.** Контраст-аудит палитр (дейтеранопия), требуемый DEC-0028 п.4, в evidence не подтверждён — провести до открытия тем игрокам (или подтвердить логом).
- **Приборные гейты (не выполнены в этом прогоне):** Step 6+ на Xiaomi 2209116AG (p50 ≤26.1 мс), аудио-прослушивание (дакинг/громкости/MIUI), Spotify не на паузе, KEEP_SCREEN_ON и диалог выхода на устройстве, лестница хаптики.
- Содержимое worktree: Gemini скопировал `docs/research/01-04` в свой worktree (untracked) — решить, оставлять ли их в ветке.

## 4. Вердикт

**ACCEPT (код-уровень) с условиями.** P0/P1-дефектов кода не найдено; все 22 пункта реализованы, тесты и analyze воспроизведены независимо, evidence сходится. До приёмки владельцем: закрыть P2-1/P2-2 (мини-патч), выполнить контраст-аудит (P1-процесс) и приборные гейты. После этого — решение владельца о коммите ветки `g1-gameplay`.

## 5. Предлагаемый мини-патч (отдельным коротким прогоном Gemini)

1. Изотропная тряска для All Clear (переиспользовать хелпер из B6).
2. `git checkout -- apps/mobile/windows/flutter/generated_*` (снять шум pub get).
3. Контраст-аудит трёх тем с логом (симуляция дейтеранопии + проверка пустых ячеек на солнце) — приложить скриншоты/числа.
