{
  "name": "lumina-blocks-functions",
  "description": "Cloud Functions for Lumina Blocks (purchase verification, entitlements).",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:verifyPurchase",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0",
    "googleapis": "^144.0.0"
  }
}
